import React from 'react/addons';
let TestUtils = React.addons.TestUtils;
import {{exposedComponent}} from '../../main';
import { ok, equal, strictEqual, throws } from 'assert';

describe('{{name}}', function () {
  it('should have tests', function () {
    ok(false);
  });
});
